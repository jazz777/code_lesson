git

初期設定

git config --global user.name "John Doe"
git config --global user.email johndoe@example.com
git config --global core.editor emac

                                              
git checkout -- file1


git log    履歴のみ表示
git log -p 差分も表示


debian→ubuntu 
uname -a で区別可能

クラウドVM
vncを利用したリモートアクセス（GUI）










